services:
  web:
    build:
      context: ..
      dockerfile: ./apps/web/Dockerfile
    container_name: fod-web-${COMPOSE_ENV:-dev}
    environment:
      PORT: ${WEB_PORT:-3000}
    restart: unless-stopped
