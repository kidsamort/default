services:
  nginx:
    image: nginx:alpine
    container_name: fod-nginx-${COMPOSE_ENV:-dev}
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "${NGINX_PORT:-80}:80"
      - "${NGINX_SSL_PORT:-443}:443"
    restart: unless-stopped
